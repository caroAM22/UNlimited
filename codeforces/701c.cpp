#include <bits/stdc++.h>
using namespace std;

#define sz(x) ((int) x.size())

int main(){
    int n; cin >> n;
    string s; cin >> s;
    set<char> se;
    for(char &c: s){
        se.insert(c);
    }
    int i=0; int j=0;
    set<char> s1;
    while(j<n){
        
    }
    return 0;
}